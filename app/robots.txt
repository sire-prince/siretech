// app/robots.ts
import { MetadataRoute } from 'next'

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/private/',
    },
    // Fix this line to point to your specific site:
    sitemap: 'https://siretech.vercel.app',
  }
}
